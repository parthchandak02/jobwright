# Agent Handoff — jobwright (Pick Back Up From Here)

- **Written:** 2026-09-17
- **Repo:** `/Volumes/ExternalSSD/Projects/jobwright` (multi-profile; **active user: `richa`**, profile dir `users/richa`)
- **NOTE — SSD down on write day:** This handoff could not be written into the repo (SSD threw intermittent `Interrupted system call`). It is staged in `jobwright-docs-staging/` — see `RESTORE.md` to land it in-repo once the SSD is healthy.

This is the "pick back up from here" doc for the next agent. Read it before doing anything else.

---

## 1. Current working state

- Live site: `jobwright.parthchandak.info` (down on 2026-09-15, mitigated same day; see the incident doc). **Repo is on the flaky ExternalSSD volume — health-check before using.**
- App + tunnel processes: `jobwright-api` and `jobwright-tunnel` under pm2 (verified live via `pm2 list` on 2026-09-17: `jobwright-api` up ~26h, `jobwright-tunnel` up ~46h; also ~15 stale one-shot `jw-*` helper apps, stopped, safe to prune with `pm2 delete`).
- `apply_enabled` is **false** — nothing was set to auto-apply; the pipeline currently stops at "prepare" (tailored resume + cover letter + PDF) and delivers results to WhatsApp.
- Scoring → prepare path is working end-to-end (Sep 16 deliverable: 10 scored jobs into prepare, see below).

## 2. Recent config changes

### 2a. `users/richa/searches.yaml` rewrite (Sep 16) — excludes + queries

Driven by Richa's feedback. She reviewed **220 companies and only ~20 were relevant** (reported, unverified count), so the search config was rewritten to exclude:

- **Excluded roles/areas:** fundraising, philanthropy, grant writer, grant researcher, development officer, development manager.
- **Foundation PROGRAM roles allowed** — but *no* fundraising / development / advancement duties.
- **No roles requiring:** PhD, therapist / clinical license, CPA.
- **Impact investing narrowed** to impact VCs only.
- **Chief-of-staff roles only** at mission-driven startups (seed → Series C, primarily non-tech).

### 2b. Scoring model / LLM routing

- Model: **`deepseek/deepseek-v4-flash-0731`, non-thinking** — VERIFIED via `~/.hermes/logs/agent.log` reasoning dump (Sep 16 16:56–18:27). Routing: `users/richa/.env` sets `LLM_URL=https://openrouter.ai/api/v1` + `LLM_API_KEY=<OpenRouter key>`; the brief script maps `JOBWRIGHT_LLM_MODEL=deepseek-v4-flash-0731`.
- Context: Fireworks account `parthchandak02` was **suspended (billing)** → hard `412 Precondition Failed` on every score call; Gemini fallback keys were also dead. OpenRouter was the working rescue path.
- gpt-oss* models are banned by Parth (Sep 2026).

## 3. Scoring incident (Sep 16) — summary

| Phase | Problem | Outcome |
|---|---|---|
| Billing | Fireworks account **suspended (billing)** → **412 errors on all LLM calls**. | Account reactivated same day (Parth). |
| Model | Thinking-mode models (glm + deepseek reasoning) burned ~5k reasoning tokens per batch, returned empty content, triggered "Empty LLM response after 3 attempts → sequential fallback" retry storms. | Switched to **non-thinking** deepseek-v4-flash-0731 via OpenRouter. |
| Result | Scoring pipeline restored. | **269 jobs scored in ~15 min** (previously ~2 hrs). **10 jobs passed scoring** (min score 7) into prepare (tailored resume + cover letter + PDF), delivered to WhatsApp — VERIFIED in Hermes session `20260817_150435_c3ac4e` (delivery message, Sep 16 ~18:27 PT). |
| Quality | Zero fundraising titles in the top batch. | Two title leaks patched into `exclude_titles`: **`grant researcher`** and **`development manager`** (patch confirmed in agent.log reasoning dump; not re-read from the repo — SSD unreadable). |

## 4. Open threads (act on these)

### (a) SSD disconnect root-cause research — NEVER DOCUMENTED
On Sep 15 a `/goal` raised sub-agents on *why `/Volumes/ExternalSSD` keeps disconnecting (common Mac issue? resolutions?)*. **Findings were never written into the repo.** Parth explicitly asked to document the root cause. **This is a priority open thread** — the SSD is still throwing `Interrupted system call` (Sep 15 14:17, Sep 16 21:51 log entries, and again all day Sep 17 during this handoff). See incident doc.

### (b) TypeSafe AI evaluation — RESOLVED: SKIP (2026-09-17)
Parth asked whether **TypeSafe AI** (`github typesafe-ai/skills`) would work for job assessment; a day earlier (2026-09-16 22:15:36, agent.log line 6896) he asked in DM: "from typesafe ai is there the jev model on firewroks". The Sep 17 group question **failed processing** because Fireworks returned `400` validation errors (`Extra inputs are not permitted, field: messages[N].reasoning_details`) when replaying the session's history — thinking-mode reasoning traces persisted in session history get rejected on replay (agent.log lines 7698+; same error class seen since Sep 15). This also explains the unanswered /summarize and /new messages at 12:05–12:07. **Not TypeSafe-related.**

Verdict from the evaluation (subagent + web sources, medium confidence): **SKIP.** TypeSafe's "Jev" (released Sep 14 2026, early access) is a classification/scoring API — it maps only to jobwright's scoring step, has no independent evidence of beating the current non-thinking deepseek scoring (269 jobs/~15 min), adds a second proprietary provider right after the Fireworks suspension incident, and does no text generation (resumes/cover letters still need an LLM). Revisit only if scoring *quality* (not speed) becomes the problem. Evidence: https://github.com/typesafe-ai/skills, https://typesafe.ai/blog/introducing-system-one-models-and-jev, https://docs.typesafe.ai/introduction/quickstart

### (c) Richa's feedback task queue (from `/q` command)
1. **Complete the last take** (last recorded take/round) that is still open.
2. Then **add Richa's feedback (the `/q` items) as concrete tasks to the todo list**, so feedback is tracked, not lost.
   - Feedback context: the searches.yaml rewrite above (excludes + role sharpening) is the concrete product of the Sep 16 feedback round.

---

## 5. Verification notes

- Numbers marked **reported, unverified** were not independently confirmable in accessible logs at write time (the repo log `users/richa/logs/brief_20260916.log` is on the SSD and was unreachable).
- Confirmed via `~/.hermes/logs/agent.log`: Error 1033 message (line 2964, Sep 15 13:54), SSD `Interrupted system call` failures (lines 3080/3125 Sep 15, 6827 Sep 16 21:51), the Fireworks 412 suspension + OpenRouter rescue + 269-job scoring chain (Sep 16 reasoning dump), and the TypeSafe/Jev DM (line 6896).
- Confirmed via Hermes session `20260817_150435_c3ac4e`: 10-job delivery with resume + cover letter + PDF (Sep 16 ~18:27 PT).
- **Do not claim** the unverified counts (220/20) as verified facts in downstream communications.
- **Session-history hazard:** any Hermes session whose history contains `reasoning_details` from thinking-mode turns will 400 on Fireworks replay. If a group session goes "not processed", check for this error class before blaming the model or network.
