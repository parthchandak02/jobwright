---
name: commit-and-push
description: Deprecated alias. Use commit-push-deploy instead.
---

# Commit and Push (alias)

This skill was renamed to **[commit-push-deploy](../commit-push-deploy/SKILL.md)**.

- **Commit only:** invoke `commit-push-deploy`; push and deploy phases are skipped unless requested.
- **Full ship:** `/commit-push-deploy` runs commit, push (when asked), and conditional dashboard deploy.

Read [commit-push-deploy/SKILL.md](../commit-push-deploy/SKILL.md) for the full workflow.
