[project]
name = "jobwright"
version = "0.4.0"
description = "AI-powered job application pipeline with Cursor/Hermes agent support"
readme = "README.md"
license = "AGPL-3.0-only"
requires-python = ">=3.11"
authors = [{name = "Pickle-Pixel"}, {name = "parthchandak"}]
keywords = ["job-search", "automation", "ai", "resume", "apply", "job-application"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business",
]

dependencies = [
    "typer>=0.12.0",
    "click>=8.0,<8.2",
    "rich>=13.0",
    "httpx>=0.24",
    "beautifulsoup4>=4.12",
    "python-docx>=1.1",
    "playwright>=1.40",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "pandas>=2.0",
    "cursor-sdk>=0.1.0",
    ]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.1"]

[project.scripts]
jobwright = "jobwright.cli:app"

[project.urls]
Homepage = "https://github.com/parthchandak02/jobwright"
Repository = "https://github.com/parthchandak02/jobwright"
Upstream = "https://github.com/Pickle-Pixel/ApplyPilot"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jobwright"]

[tool.hatch.build]
artifacts = ["src/jobwright/config/*.yaml", "src/jobwright/config/*.json"]

[tool.ruff]
target-version = "py311"
line-length = 120
